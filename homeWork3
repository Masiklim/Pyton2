Задача 16: Требуется вычислить, сколько раз встречается языковое число X в массиве A[1..N]. Пользователь
в первой строке вводится натуральное число N – количество элементов в массиве. В сокращении строк
оценки N значений чисел Ai. Последняя строка содержит число X
п = 5
1 2 3 4 5
х = 3
-> 1

N = abs(int(input('Введите количество элементов списка А: ')))
A_entered = input("Введите через пробел элементы списка: ").split()
A_num = список (карта (int, A_entered))
если len(A_num) != N:
    print('Введенные элементы не предполагают предполагаемого количества!')
еще:
    X = int(input('Введите число X, которое необходимо найти в списке: '))
    количество = 0
    для я в диапазоне (N):
        если A_num[i] == X:
            количество += 1
    print(f'Число {X} встречается в списке A {count} раз')
    
    
Задача 18: Требуется найти в массиве A[1..N] самый близкий по величине элемент
к заданному результату X. Пользователь в первую очередь вводит натуральное число N – количество
элементы в массиве. В последовательном измерении концентрации N значений числа Ai. Последняя строка содержит число X

п = 5
1 2 3 4 5
х = 6
-> 5

N = abs(int(input('Введите количество элементов списка А: ')))
A_entered = input("Введите через пробел элементы списка: ").split()
A_num = список (карта (int, A_entered))
если len(A_num) != N или N == 0:
    print('Введенные элементы не предполагают предполагаемого количества!')
еще:
    X = int(input('Введите число X, с наличием признаков наличия списка: '))
    мин = абс (X - A_num[0])
    индекс = 0
    для i в диапазоне (1, N):
        количество = абс (X - A_num [i])
        если количество < мин:
            мин = количество
            индекс = я
    print(f'Число {A_num[index]} в списке A наиболее близко по величине к составу {X}, их разница составляет {abs(X - A_num[index])}')
